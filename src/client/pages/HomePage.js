import React from 'react';

const Home = () => {
    return (
        <div>
            <div>SK in progress in SSR </div>
            <button onClick={() => console.log('Hi there')}>Press me!</button>
        </div>
    )
};

export default Home;